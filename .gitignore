# ─────────────────────────────────────────────────────────────
#  .gitignore —— 帮帮AD域管理工具
#
#  分四段。第二、三段是红线：
#    ② 程序运行期产生的数据（连接配置 / 审计 / 日志）—— 盘上任何一份都不许进库；
#    ③ 含真实环境信息的内部件 + 未脱敏的证据目录。
#  ⚠️ 第三段里的 `tests/` `tools/` 是**取舍**，不是技术必需：
#     它们里面是"拿真实域控现身说法"的验收脚本与实测探针，
#     本项目的脱敏守卫**有意**只守生产模块 —— 禁区词表里那条"真实账号名"的理由原文写着
#     「供验收脚本现身说法，不许进生产模块」。
#     这条取舍只在"不进库"的前提下成立 ⇒ 默认不进库。
#     要把它们也一并公开 ⇒ 把那两行删掉，并先做完脱敏与真值复核（另开一轮）。
# ─────────────────────────────────────────────────────────────

# ① 可再生的构建与运行产物（体积大、每台机器不一样、没有版本价值）
.venv/
build/
dist/
build_onefile/
#    Release 附件（zip 本体走 GitHub Release，不进仓库）
release/
__pycache__/
*.py[cod]
*.spec.bak

# ② 🔴 运行时数据 —— **绝不许进库**
#    连接配置里可能有：域控地址、绑定账号、以及 DPAPI 加密后的口令密文。
#    它本来就**不在**本目录（程序把它写到 %APPDATA%\AD域管理工具\），
#    这一段是**防呆**：万一有人把本目录当数据目录、或手工拷了一份进来。
config.json
config.json.bak-*
config.json.bad-*
audit-*.jsonl
logs/
*.log

# ③ 🔴 含真实环境信息的内部件 / 未脱敏的证据目录
#    交付说明本身就是"把真值列出来给你看"的文档 ⇒ 天然含真值，不进库。
交付说明-*.md
overview*.md
#    证据目录：验收脚本与实测探针要拿真实域控"现身说法"（见文件头说明）。
tests/
tools/

# ④ 编辑器 / 系统垃圾
.vscode/
.idea/
Thumbs.db
desktop.ini
*~
*.tmp
